import * as React from 'react';
import './App.css';

import { FontSizes } from '@fluentui/theme';

const Header = () => (
    <div>
        <div style={{ fontSize: FontSizes.size32 }}>
            logo + Network Statale Informatica
        </div>
    </div>
    // + parte pivot dell'header

);

export default Header;