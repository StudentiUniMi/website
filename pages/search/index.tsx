import MainContainer from "@/components/main-container"
import { Box, Container } from "@chakra-ui/react"

const SearchPage = () => {
  return (
    <MainContainer as={Container}>
      <Box pt={12}>Search page</Box>
    </MainContainer>
  )
}

export default SearchPage
